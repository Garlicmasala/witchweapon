version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      FLASK_ENV: development
      PYTHONUNBUFFERED: 1
    volumes:
      - .:/app
    command: python web_app.py
